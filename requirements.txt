# Asta Powerproject MCP Server - Requirements
# Install with: pip install -r requirements.txt

# MCP Protocol SDK
mcp>=1.0.0

# Input validation
pydantic>=2.0.0

# Project file reading (supports .pp, .mpp, .xml, .xer)
mpxj>=1.0.0

# GUI Automation (for controlling Asta on Windows desktop)
pyautogui>=0.9.54
pywinauto>=0.6.8

# HTTP client (for potential API calls)
httpx>=0.24.0
